apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: order-helidon
spec:
  selector:
    matchLabels:
      app: order
#  namespaceSelector:
#    matchNames:
#    - datademo
  endpoints:
  - port: http
#    interval: 30s